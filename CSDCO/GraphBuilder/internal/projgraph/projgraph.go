package projgraph

import (
	"log"
	// "opencoredata.org/ocdGarden/CSDCO/GraphBuilder/internal/jld"
)

// BuildProjGraph is designed to collec the information from the CSV, form
// a struct an generate the JDSL
func BuildProjGraph() string {
	log.Println("In projgraph builder")

	// TODO:
	// read in the CSV data
	// find the unique proj names
	// get the project level metadata (not the holeid level)
	// this might be a small amount of the data

	// call jld.BuildProj(dg)

	return "done"

}
